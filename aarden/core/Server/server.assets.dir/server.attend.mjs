


// attend :: Server : import related `.mjs` (supply if exists) and attend Server
// ----------------------------------------------------------------------------------------------------------------------------
    Script.supply( `${'path'}` ).then(()=>
    { Server.attend(`${'full'}`) });
// ============================================================================================================================



/*
*/
